version: "3.8"

services:
  coldscout:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./coldscout.db:/app/coldscout.db
      - ./tmp:/app/tmp
    restart: unless-stopped
